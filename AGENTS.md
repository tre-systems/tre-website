# Agent Rules

This file contains guidance for AI agents working on the Total Reality
Engineering website.

## Current Direction

The TRE website is a proof-of-work portfolio. Its job is to make Rob's work
look credible to clients, employers, collaborators, and people checking whether
he can build real software.

Current priority noted on 2026-05-29:

- Keep improving the project cards as a portfolio surface.
- Make sure the live site shows the right flagship work: Talata, Comprehendo,
  Antenna, Acto, and the strongest public demos.
- Review cards one by one for description quality, screenshot quality, topic
  tags, live URL usefulness, and whether the project tells a convincing story.
- Treat the portfolio as preparation for later CV, LinkedIn, and work-search
  updates.

## Project Card Rules

- The grid is one flat, ranked list — not grouped into sections. Cards run
  flagship-first (`FLAGSHIP_REPOS`, in that order) then by most recent push, so
  the strongest work always leads. The type of work (`CATEGORY_OF`) is shown as
  a small label on each card, not as a section heading. Promote or demote a
  project by editing `FLAGSHIP_REPOS`.
- Public repositories can show both website and GitHub links when available.
- Private repositories can appear only when they have a public TRE-owned
  homepage, description, topics, and a README image suitable for the card.
- Private repository cards must not expose private GitHub links.
- Private project screenshots are cached under `generated/project-images/` so
  the public site does not depend on private GitHub asset access. The cache file
  is keyed by GitHub repo name, so it stays named after the repo even when the
  card displays a different public name.
- When a project's public name or URL moves ahead of the not-yet-renamed GitHub
  repo, add the repo to `DISPLAY_NAME` and/or `HOMEPAGE_OVERRIDE` in
  `build-projects.js` (and add the new host to `APPROVED_PRIVATE_PROJECT_HOSTS`
  if it is a private repo). The `writeo` repo ships as **talata** at
  `https://talata.app/` this way.
- The scheduled project refresh needs a valid `PROJECTS_GITHUB_TOKEN` secret
  with read access to private `tre-systems` repositories. Without it, the
  workflow skips regeneration and keeps the committed site live.
- The daily GitHub Actions schedule intentionally uses an off-minute UTC cron
  rather than a top-of-hour schedule, because top-of-hour scheduled runs have
  repeatedly started several hours late.
- Scheduled runs commit and deploy only when the generated project grid or
  cached project images change. Push and manual runs may still deploy during
  active work.

## Near-Term Backlog

1. Card lead images (audited June 2026 — most are strong: real product UIs,
   branded title cards, or striking generative visuals). Worth improving in the
   source-repo READMEs:
   - `cefr-workshop`, `github-org-metrics` — raw terminal/table shots; usable but
     could be nicer (the cefr-workshop shot also leaks a local `/Users/...` path).
   - `galacto`, `pongo`, `planeto` read dark/minimal, but that matches the works
     and the site's dark theme — leave as-is.
   To swap a card image, make a good screenshot the FIRST image in that repo's
   README (the generator caches it); follow the `antenna` pattern.
2. Optional hero/conversion enhancements (deferred, agreed June 2026):
   - A "Selected Work" tier surfacing the top 3–4 projects above the categories.
   - A subtle "available for consulting" CTA near the contact section.
3. Check whether any other private project should appear via a public homepage
   on an approved host (`antenna` uses a `*.tre.systems` URL; `talata` uses
   `talata.app`).
4. Keep `index.html`'s project grid generated by `build-projects.js`; only the
   head/hero/about/footer (outside the grid markers) are hand-edited.

## Verification

- Run `PROJECTS_GITHUB_TOKEN="$(gh auth token)" npm run build` when refreshing
  private project cards locally.
- Run `git diff --check` before committing generated site changes.
- Check the live site after deployment for the important cards and links.
