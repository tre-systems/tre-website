# Agent Rules

This file contains guidance for AI agents working on the Total Reality
Engineering website.

## Current Direction

The TRE website is a proof-of-work portfolio. Its job is to make Rob's work
look credible to clients, employers, collaborators, and people checking whether
he can build real software.

Current priority noted on 2026-05-29:

- Keep improving the project cards as a portfolio surface.
- Make sure the live site shows the right flagship work: Writeo, Antenna, Acto,
  and the strongest public demos.
- Review cards one by one for description quality, screenshot quality, topic
  tags, live URL usefulness, and whether the project tells a convincing story.
- Treat the portfolio as preparation for later CV, LinkedIn, and work-search
  updates.

## Project Card Rules

- Public repositories can show both website and GitHub links when available.
- Private repositories can appear only when they have a public `*.tre.systems`
  URL, description, topics, and a README image suitable for the card.
- Private repository cards must not expose private GitHub links.
- Private project screenshots are cached under `generated/project-images/` so
  the public site does not depend on private GitHub asset access.
- The scheduled project refresh needs a valid `PROJECTS_GITHUB_TOKEN` secret
  with read access to private `tre-systems` repositories. Without it, the
  workflow skips regeneration and deploys the committed site.

## Near-Term Backlog

1. Review the live cards for Writeo, Antenna, and Acto first.
2. Improve any weak screenshots, README lead images, descriptions, and topics
   in the source repositories rather than hard-coding portfolio copy here.
3. Confirm each important project has a useful public demo URL.
4. Check whether any private project should appear through a public
   `*.tre.systems` URL.
5. Keep `index.html` generated by `build-projects.js`; do not manually edit the
   project grid unless debugging a generator issue.

## Verification

- Run `PROJECTS_GITHUB_TOKEN="$(gh auth token)" npm run build` when refreshing
  private project cards locally.
- Run `git diff --check` before committing generated site changes.
- Check the live site after deployment for the important cards and links.
